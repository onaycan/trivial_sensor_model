#pragma once
#include <iostream>
#include <string>
#include <vector>

/*! \file miscellaneous.h Containsglobal functions which may save a little more spacess.*/

using namespace std;
typedef unsigned short int type_scaled_obj_coords;

string tabs(int i);
int Ltwnorm(vector<type_scaled_obj_coords> c);

